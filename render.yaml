services:
  - type: web
    name: digital-services-app
    env: node
    rootDirectory: .
    buildCommand: pnpm install && pnpm run build
    startCommand: pnpm start
    envVars:
      - key: NODE_ENV
        value: production
    healthCheckPath: /
    plan: free
version: "1"
